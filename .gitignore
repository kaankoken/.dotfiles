.config/nvim.bak

# Claude Code
.claude/
.claude-enterprise/
.claude.json*

# Firebase (contains auth tokens)
.config/configstore/
firebase-debug.log

# Nushell
.config/nushell/history.*

# Shell history databases
.config/history.*

# Radicle (only config.json is tracked; keys + local state must never be committed)
.radicle/*
!.radicle/config.json
