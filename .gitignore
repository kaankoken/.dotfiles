.DS_Store
**/.DS_Store

# Pi
/.pi/context-mode/sessions
/.pi/context-mode/content
/.pi/agent/sessions/
/.pi/agent/auth.json
/.pi/agent/cursor-sdk-context-windows.json
/.pi/agent/cursor-sdk-model-list.json
/.pi/agent/models-store.json
/.pi/agent/mcp-cache.json
/.config/pi/web-search-cache
/.pi/workflows
/specs/
/.config/pi-hashline-edit-pro/sessions


# Third-party skill clones (stow → ~/.agents/…)
/.agents/google-skills/
/.agents/google-cloud-storage/

# gh — per-machine accounts (auth switch dirties this)
/.config/gh/hosts.yml

# SSH — deny by default so private keys can never be added
/.ssh/*
!/.ssh/config

# History
*.sqlite3*
*.sqlite*

# cargo-dist installer state (rewritten on self-update)
**/*-receipt.json

# Helix
/.config/helix/runtime/

# git

.worktrees
worktrees

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
/.beads
